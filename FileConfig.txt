# Revisions of the config files will have embedded versions.
# Updates will, unfortunately, overwrite _this_ file. But at least you won't lose your data.
# As always the first row is filled with column headers, do not change or delete this
GridActors.txt | IndustryRecipes.txt | Resources.txt | TechUnlocks.txt | Localizations

# Your filenames below. Edit these to revert to previous or custom files.
# On the off chance that you want to add your own localizations, comma separate
# and make the filename the language code, e.g. 'en.txt, de.txt' to add German.
# It seems filenames can't have spaces, so beware.
GridActors.0.1.0.0.txt | IndustryRecipes.0.1.0.0.txt | Resources.0.1.0.0.txt | TechUnlocks.0.1.0.0.txt | en.txt, zh.txt
